import React from 'react';
import StudentDashboard from '../student/StudentDashboard';

const TeacherDashboard = () => <StudentDashboard basePath="/teacher" teacherMode />;

export default TeacherDashboard;
